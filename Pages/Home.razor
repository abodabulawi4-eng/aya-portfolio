@page "/"

<div class="hero-section">
    <div class="container">
        <div class="hero-content">
            <div class="hero-text">
                <h1>Aya zeyad Alqennah</h1>
                <h2>Cyber Security Student & Analyst</h2>
                <p class="hero-description">
                    I'm a dedicated Cybersecurity student with hands-on experience in network security, penetration testing, and risk assessment.
                </p>
                <div class="hero-buttons">
                    <a href="/projects" class="btn btn-primary">View My Projects</a>
                    <a href="/contact" class="btn btn-secondary">Get In Touch</a>
                </div>
                <div class="hero-stats">
                    <div class="stat">
                        <span class="stat-number">+3</span>
                        <span class="stat-label">Years Learning</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">+10</span>
                        <span class="stat-label">Projects Completed</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">100%</span>
                        <span class="stat-label">Commitment</span>
                    </div>
                </div>
            </div>
            <div class="hero-image">
                <div class="image-container">
                    <img src="images/WhatsApp%20Image%202025-09-23%20at%2018.04.42_2e368845.jpg"
                         alt="Aya zeyad Alqennah - Cyber Security Analyst"
                         onerror="this.style.display='none';
this.nextElementSibling.style.display='flex';" />
                    <div class="image-placeholder">
                        <span>Aya Alqennah</span>
                    </div>
                </div>
                <div class="floating-elements">
                    <div class="floating-element element-1">Nmap</div>
                    <div class="floating-element element-2">Pen-Test</div>
                    <div class="floating-element element-3">Linux</div>
                    <div class="floating-element element-4">Firewall</div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="skills-preview">
    <div class="container">
        <h3>Technologies I Work With</h3>
        <div class="skills-grid">
            <div class="skill-item">
                <div class="skill-icon">🛡️</div>
                <span>Network Security</span>
            </div>
            <div class="skill-item">
                <div class="skill-icon">🕵️</div>
                <span>Ethical Hacking</span>
            </div>
            <div class="skill-item">
                <div class="skill-icon">🐧</div>
                <span>Kali Linux</span>
            </div>
            <div class="skill-item">
                <div class="skill-icon">🔍</div>
                <span>Wireshark</span>
            </div>
            <div class="skill-item">
                <div class="skill-icon">🎯</div>
                <span>Penetration Testing</span>
            </div>
            <div class="skill-item">
                <div class="skill-icon">📜</div>
                <span>Scripting</span>
            </div>
        </div>
    </div>
</div>

<style>
    /* 🌟 Hero Section */
    .hero-section {
        /* استخدمنا لون الخلفية الأساسي */
        background: var(--bg-primary);
        /* تدرج الخلفية الناعم */
        background-image: linear-gradient(160deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: 2rem 0;
        margin-top: -80px;
        padding-top: 80px;
        position: relative;
        overflow: hidden;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
    }

    .hero-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
    }

    .hero-text h1 {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        /* استخدمنا لون Rose Gold للعناوين */
        color: var(--accent-rose-gold);
        font-weight: 700;
        letter-spacing: 0.5px; /* مسافة بسيطة بين الحروف */
    }

    .hero-text h2 {
        font-size: 1.8rem;
        /* استخدمنا لون Rose Gold */
        color: var(--accent-rose-gold);
        margin-bottom: 1.5rem;
        font-weight: 600;
    }

    .hero-description {
        font-size: 1.1rem;
        line-height: 1.8;
        margin-bottom: 2.5rem;
        /* لون النص الرمادي الغامق */
        color: var(--text-primary);
        max-width: 500px;
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        margin-bottom: 3rem;
        flex-wrap: wrap;
    }

    .btn {
        display: inline-block;
        padding: 14px 32px;
        border: none;
        border-radius: 8px; /* زوايا ناعمة */
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        text-align: center;
        min-width: 160px;
    }

    .btn-primary {
        /* تدرج الألوان الزهري اللؤلؤي */
        background-image: linear-gradient(90deg, var(--accent-gradient-start) 0%, var(--accent-gradient-end) 100%);
        color: var(--text-on-accent);
    }

        .btn-primary:hover {
            transform: translateY(-3px);
            /* ظل وردي ناعم (توهج) كما طلبت */
            box-shadow: 0 10px 25px var(--shadow-color);
            filter: brightness(1.05); /* إضاءة خفيفة */
        }

    .btn-secondary {
        background: var(--btn-secondary-bg);
        /* إطار زهري باهت */
        border: 2px solid var(--btn-secondary-border);
        color: var(--accent-rose-gold);
    }

        .btn-secondary:hover {
            background: var(--btn-secondary-hover);
            color: var(--text-primary);
            border-color: var(--accent-rose-gold);
            transform: translateY(-3px);
        }

    /* 📊 Statistics */
    .hero-stats {
        display: flex;
        gap: 2rem;
        margin-top: 2rem;
    }

    .stat {
        text-align: center;
    }

    .stat-number {
        display: block;
        font-size: 2rem;
        font-weight: 700;
        /* استخدمنا لون Rose Gold للأرقام */
        color: var(--accent-rose-gold);
        margin-bottom: 0.5rem;
    }

    .stat-label {
        font-size: 0.9rem;
        color: var(--text-muted);
    }

    /* 🖼️ Image Section */
    .hero-image {
        position: relative;
        display: flex;
        justify-content: center;
    }

    .image-container {
        position: relative;
        width: 100%;
        max-width: 400px;
        transition: all var(--transition-fast);
    }

        .image-container:hover {
            /* توهج خفيف عند المرور */
            transform: scale(1.02);
            box-shadow: 0 15px 40px var(--shadow-color);
        }

        .image-container img {
            width: 100%;
            height: 500px;
            object-fit: cover;
            border-radius: 20px; /* زوايا ناعمة */
            /* ظل خفيف بدلاً من التوهج */
            box-shadow: 0 10px 30px var(--shadow-color);
            /* إطار Rose Gold ناعم */
            border: 2px solid var(--accent-rose-gold);
        }

    .image-placeholder {
        width: 100%;
        height: 500px;
        /* تدرج لوني فخم */
        background: linear-gradient(135deg, var(--accent-gradient-start), var(--accent-gradient-end));
        border-radius: 20px;
        display: none;
        align-items: center;
        justify-content: center;
        color: var(--text-on-accent);
        font-size: 1.5rem;
        font-weight: 600;
        box-shadow: 0 10px 30px var(--shadow-color);
    }

    /* 🎯 Floating Elements */
    .floating-elements {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
    }

    .floating-element {
        position: absolute;
        /* خلفية شفافة هادئة */
        background: rgba(248, 246, 249, 0.5); /* --bg-primary with opacity */
        /* لون الخط Rose Gold */
        color: var(--accent-rose-gold);
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 600;
        /* إطار ناعم */
        border: 1px solid var(--border-color);
        backdrop-filter: blur(10px);
        animation: float 6s ease-in-out infinite;
    }

    .element-1 {
        top: 10%;
        left: 10%;
        animation-delay: 0s;
    }

    .element-2 {
        top: 20%;
        right: 10%;
        animation-delay: 1.5s;
    }

    .element-3 {
        bottom: 30%;
        left: 5%;
        animation-delay: 3s;
    }

    .element-4 {
        bottom: 15%;
        right: 15%;
        animation-delay: 4.5s;
    }

    @@keyframes float {
        0%, 100% {
            transform: translateY(0px) rotate(0deg);
        }

        50% {
            transform: translateY(-20px) rotate(5deg);
        }
    }

    /* 🛠️ Skills Preview Section */
    .skills-preview {
        background: var(--bg-secondary); /* لون الخلفية الثانوي */
        padding: 4rem 0;
        border-top: 1px solid var(--border-color);
    }

        .skills-preview h3 {
            text-align: center;
            color: var(--text-primary);
            margin-bottom: 3rem;
            font-size: 2rem;
        }

    .skills-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 1.5rem;
        max-width: 800px;
        margin: 0 auto;
    }

    .skill-item {
        background: var(--bg-primary); /* لون الخلفية الأساسي */
        padding: 1.5rem;
        border-radius: 12px; /* زوايا ناعمة */
        text-align: center;
        border: 1px solid var(--border-color);
        transition: all 0.3s ease;
    }

        .skill-item:hover {
            transform: translateY(-5px);
            /* إطار أفتح عند المرور */
            border-color: var(--accent-rose-gold);
            /* ظل وردي خفيف جداً */
            box-shadow: 0 10px 25px var(--shadow-color);
        }

    .skill-icon {
        font-size: 2rem;
        margin-bottom: 0.5rem;
    }

    .skill-item span {
        color: var(--text-secondary);
        font-weight: 500;
    }

    /* 📱 Responsive Design */
    @@media (max-width: 768px) {
        .hero-content {
            grid-template-columns: 1fr;
            text-align: center;
            gap: 2rem;
        }

        .hero-text h1 {
            font-size: 2.5rem;
        }

        .hero-text h2 {
            font-size: 1.4rem;
        }

        .hero-description {
            font-size: 1rem;
        }

        .hero-buttons {
            justify-content: center;
        }

        .btn {
            min-width: 140px;
            padding: 12px 24px;
        }

        .hero-stats {
            justify-content: center;
            gap: 1.5rem;
        }

        .stat-number {
            font-size: 1.5rem;
        }

        .skills-grid {
            grid-template-columns: repeat(2, 1fr);
        }

        .floating-element {
            display: none;
        }
    }

    @@media (max-width: 480px) {
        .hero-text h1 {
            font-size: 2rem;
        }

        .hero-buttons {
            flex-direction: column;
            align-items: center;
        }

        .btn {
            width: 100%;
            max-width: 250px;
        }

        .hero-stats {
            flex-direction: column;
            gap: 1rem;
        }

        .skills-grid {
            grid-template-columns: 1fr;
        }
    }
</style>